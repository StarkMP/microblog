"use client";

import { createTheme } from "@mantine/core";

export const theme = createTheme({
  other: {
    fontWeight: {
      light: 300,
      normal: 400,
      medium: 500,
      semiBold: 600,
      bold: 700,
    },
  },
});
