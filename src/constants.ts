// Cookies
export const ACCESS_TOKEN_COOKIE_NAME = "accessToken" as const;
export const REFRESH_TOKEN_COOKIE_NAME = "refreshToken" as const;

// Numbers
export const ACCESS_TOKEN_EXPIRES_IN_MINS = 60 as const;
